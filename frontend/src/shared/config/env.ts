/**
 * Конфигурация времени выполнения.
 *
 * Раньше origin API был зашит прямо в компонент страницы, из-за чего приложение
 * невозможно было развернуть нигде, кроме ноутбука разработчика.
 */
export const API_BASE_URL =
  process.env.NEXT_PUBLIC_API_URL?.replace(/\/+$/, "") ?? "http://localhost:8000";

/** Как часто дашборд перепроверяет файлы, которые ещё обрабатываются. */
export const PROCESSING_POLL_INTERVAL_MS = 2000;
